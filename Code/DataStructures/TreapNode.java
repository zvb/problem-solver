public class TreapNode {
    final int x, y;
    TreapNode left, right;
    public TreapNode(int x, int y) {
        this.x = x;
        this.y = y;
    }
    void split(int x, TreapNode l, TreapNode r) {
        if (this.x < x) {
            Treap rl = new Treap
            Treap right = new Treap(root.right);
            right.split(x, )
        }
    }
        
}