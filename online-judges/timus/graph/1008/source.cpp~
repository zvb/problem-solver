#include <iostream>
#include <cstdlib>
#include <cstdio>
#include <algorithm>

using namespace std;

int n, m;
int g[15][15];

int main() {
  scanf("%d", &n);
  for (int i = 0 ; i < n ; i ++) {
    int x, y;
    scanf("%d%d", &x, &y);
    g[x][y] = 1;
  }
  

    
  return 0;
}
