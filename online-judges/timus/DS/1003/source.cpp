#include <iostream>
#include <cstdlib>
#include <vector>

using namespace std;

const int N = 5555;

int n, l[N], r[N];

int main() {
  while (1) {
    scanf("%d", &n);
    if (n == -1)
      break;
    
  }
  return 0;
}
